// React's <ViewTransition> (used for the desk ↔ account transition) is a canary API:
// Next's vendored React ships it, @types/react types it behind this reference.
/// <reference types="react/canary" />
